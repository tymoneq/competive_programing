#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
constexpr int M = 1010, MOD = 1e9 + 7;
ll dp[M];
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, a, b, c, d;
    cin >> n >> a >> b >> c >> d;
}