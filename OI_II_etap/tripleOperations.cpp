#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t, l, r;
    ll ans = numeric_limits<ll>::max();

    cin >> t;
    while (t--)
    {
        cin >> l >> r;

    
        
    
    }

    return 0;
}