#include <bits/stdc++.h>

using namespace std;
constexpr int M = 1e6 + 10;
int Arr[M];
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    double res;
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < q; i++)
    {
    }
    return 0;
}