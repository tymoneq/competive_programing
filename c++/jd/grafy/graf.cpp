#include <bits/stdc++.h>

using namespace std;

int main()
{
    int node;
    cin >> node;
    int edge;
    cin >> edge;
    vector<int> G[node + 1];
    vector<pair<int, int>> g[node + 1];
    vector<vector<pair<int, int>>> H[node + 1];
    pair<int, int> p(6, 5);
    pair<int, int> z;
    z = make_pair(6, 7);

    pair<int, double> b;
    b.first = 7;
    b.second = 4.44;
    pair<int, char> k;
    k = pair(2, '5');
    
    return 0;
}