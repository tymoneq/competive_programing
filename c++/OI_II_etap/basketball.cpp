#include <bits/stdc++.h>
using namespace std;
const int MAX_VAL = 1e5 + 10;
int Height_1[MAX_VAL];
int Height_2[MAX_VAL];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
        cin >> Height_1[i];
    for (int i = 0; i < n; i++)
        cin >> Height_2[i];
        
    return 0;
}