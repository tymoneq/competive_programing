#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    vector<pair<int, int>> Films(n);
    for (int i = 0; i < n; i++)
        cin >> Films[i].first >> Films[i].second;
        

    return 0;
}