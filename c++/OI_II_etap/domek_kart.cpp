#include <bits/stdc++.h>
using namespace std;
const int base = 1 << 17;
long long Tree[base << 1];
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++)
    {
    }
    return 0;
}